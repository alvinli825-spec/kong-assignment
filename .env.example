# Server
PORT=3000

# Database
DB_HOST=localhost
DB_PORT=5433
DB_USERNAME=kong
DB_PASSWORD=kong
DB_NAME=kong_services

# Auth tokens (static bearer tokens; see README for the trade-off discussion)
API_READER_TOKEN=reader-token
API_ADMIN_TOKEN=admin-token
